class Solution {
    public int lengthOfLastWord(String s) {
        s = s.trim();          // remove trailing spaces
        int n = s.length() - 1;
        int count = 0;

        while (n >= 0 && s.charAt(n) != ' ') {
            count++;
            n--;
        }

        return count;
    }
}
