class Solution {
    public int missingNumber(int[] nums) {
        int n=nums.length;
        int k=0,j=0;
        for(int i=0;i<n;i++){
            k=k^nums[i];

        }
        for(int i=1;i<=n;i++){
            j=j^i;
        }
        return j^k;
        
    }
}
